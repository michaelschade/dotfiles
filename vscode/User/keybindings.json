// Place your key bindings in this file to override the defaults
[
  /* Terminal management */
  // Create new full screen terminal
  {
    "key": "ctrl+`",
    "command": "workbench.action.terminal.new",
    "when": "terminalProcessSupported || terminalWebExtensionContributedProfile"
  },
  {
    "key": "ctrl+`",
    "command": "-workbench.action.createTerminalEditor"
  },
  {
    "key": "ctrl+`",
    "command": "workbench.action.createTerminalEditor"
  },

  // Create new terminal in panel
  {
    "key": "cmd+`",
    "command": "workbench.action.terminal.new",
    "when": "terminalProcessSupported || terminalWebExtensionContributedProfile"
  },
  {
    "key": "ctrl+w",
    "command": "workbench.action.terminal.killEditor",
    "when": "terminalEditorFocus && terminalFocus && terminalHasBeenCreated && resourceScheme == 'vscode-terminal' || terminalEditorFocus && terminalFocus && terminalProcessSupported && resourceScheme == 'vscode-terminal'"
  },

  // Toggle full screen terminal
  {
    "key": "shift+cmd+j",
    "command": "-workbench.action.search.toggleQueryDetails",
    "when": "inSearchEditor || searchViewletFocus"
  },
  {
    "key": "shift+cmd+j",
    "command": "workbench.action.toggleMaximizedPanel",
    "when": "terminal.active"
  },

  /* Debug: In addition to [shift+] F5, support [shift+] optn+enter to run/debug */
  // Start
  {
    "key": "cmd+alt+enter",
    "command": "workbench.action.debug.start",
    "when": "debuggersAvailable && debugState == 'inactive'"
  },
  // Continue
  {
    "key": "cmd+alt+enter",
    "command": "workbench.action.debug.continue",
    "when": "debugState == 'stopped'"
  },
  // Stop
  {
    "key": "shift+cmd+alt+enter",
    "command": "workbench.action.debug.stop",
    "when": "inDebugMode && !focusedSessionIsAttach"
  },

  /* Python */
  // Exec *file* in terminal
  {
    "key": "shift+cmd+enter",
    "command": "python.execInTerminal",
    "when": "editorTextFocus && !findInputFocussed && !jupyter.ownsSelection && !notebookEditorFocused && !replaceInputFocussed"
  },

  /* Text motions */

  // Allow optn+left/right to move by word part (e.g. camelCase and snake_case)
  {
    "key": "alt+left",
    "command": "cursorWordPartLeft",
    "when": "textInputFocus"
  },
  {
    "key": "alt+right",
    "command": "cursorWordPartRight",
    "when": "textInputFocus"
  },

  // But shift+optn+left/right should still select the whole word
  {
    "key": "shift+alt+left",
    "command": "cursorWordStartLeftSelect",
    "when": "textInputFocus && !editorHasSelection"
  },
  {
    "key": "shift+alt+right",
    "command": "cursorWordStartRightSelect",
    "when": "textInputFocus && !editorHasSelection"
  },

  // And allow optn+ctrl+left/right to move by word part (e.g. camelCase and snake_case)
  {
    "key": "alt+ctrl+left",
    "command": "cursorWordStartLeft",
    "when": "textInputFocus"
  },
  {
    "key": "alt+ctrl+right",
    "command": "cursorWordStartRight",
    "when": "textInputFocus"
  }
]
